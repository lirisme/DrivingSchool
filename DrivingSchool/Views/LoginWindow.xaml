<Window x:Class="DrivingSchool.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Автошкола" 
        Height="250" 
        Width="300"
        WindowStartupLocation="CenterScreen">

    <StackPanel Margin="10">
        <TextBlock Text="Вход в систему" FontSize="16" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <TextBox x:Name="LoginBox" Margin="0,0,0,10" Height="30"/>
        <PasswordBox x:Name="PasswordBox" Margin="0,0,0,15" Height="30"/>

        <Button x:Name="LoginButton" Content="Войти" Height="30" 
                Click="LoginButton_Click" Background="LightGray"/>

        <TextBlock x:Name="ErrorText" Foreground="Red" 
                   HorizontalAlignment="Center" Margin="0,10,0,0"/>
    </StackPanel>
</Window>