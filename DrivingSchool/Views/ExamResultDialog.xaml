<Window x:Class="DrivingSchool.Views.ExamResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Результат экзамена" Height="500" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📝 Ввод результатов экзамена" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="#F0F0F0" Padding="10" Margin="0,0,0,15" CornerRadius="5">
            <StackPanel>
                <TextBlock x:Name="StudentNameText" FontWeight="Bold"/>
                <TextBlock x:Name="ExamInfoText" FontSize="11" Foreground="Gray"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Дата:" VerticalAlignment="Center"/>
            <DatePicker x:Name="ExamDatePicker" Grid.Row="0" Grid.Column="1" SelectedDate="{Binding ExamDate}" Margin="5"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Результат:" VerticalAlignment="Center"/>
            <ComboBox x:Name="ResultCombo" Grid.Row="1" Grid.Column="1" Margin="5" SelectedIndex="1">
                <ComboBoxItem Content="✅ Сдан" Tag="Passed"/>
                <ComboBoxItem Content="❌ Не сдан" Tag="Failed"/>
                <ComboBoxItem Content="📅 Назначен" Tag="Scheduled"/>
            </ComboBox>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Баллы (0-100):" VerticalAlignment="Center"/>
            <Slider x:Name="ScoreSlider" Grid.Row="2" Grid.Column="1" Minimum="0" Maximum="100" 
                    Value="70" Margin="5" IsEnabled="True"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Экзаменатор:" VerticalAlignment="Center"/>
            <TextBox x:Name="ExaminerTextBox" Grid.Row="3" Grid.Column="1" Margin="5"/>
        </Grid>

        <TextBlock Grid.Row="3" Text="Ошибки (через запятую):" Margin="0,10,0,5"/>
        <TextBox x:Name="MistakesTextBox" Grid.Row="4" Height="60" TextWrapping="Wrap" 
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="5" Text="Замечания:" Margin="0,5,0,5"/>
        <TextBox x:Name="NotesTextBox" Grid.Row="6" Height="80" TextWrapping="Wrap" 
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="7" x:Name="ScoreDisplayText" Text="Баллы: 70" FontSize="11" Foreground="Gray" HorizontalAlignment="Right"/>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="SaveBtn" Content="Сохранить" Width="100" Padding="10,5" 
                    Background="#27AE60" Foreground="White" Click="SaveBtn_Click" Margin="5"/>
            <Button x:Name="CancelBtn" Content="Отмена" Width="100" Padding="10,5" 
                    Click="CancelBtn_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>